server:
  port: 8081
logging:
  level:
    root: error
dubbo:
  application:
    name: dubbo-consumer
    auto-trust-serialize-class: true
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://localhost:8848?username=nacos&password=nacos
  consumer:
    check: false
  metadata-report:
    retry-times: 3