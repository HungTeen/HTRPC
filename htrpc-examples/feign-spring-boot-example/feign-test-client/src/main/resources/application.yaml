server:
  port: 8081
logging:
  level:
    root: error
spring:
  application:
    name: test-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        service: test-consumer # 注册到 Nacos 的服务名。默认值为 ${spring.application.name}。